# R3 Tests - Environment Template
#
# This file is only needed for:
# - Running integration tests against a local backend
# - Overriding default test configuration
#
# Most tests use configuration from /config/constants.js
#
# To run tests:
#   cd r3-workspace/tests
#   npm test
#   (Usually no .env file needed!)

# =============================================================================
# TEST ENVIRONMENT
# =============================================================================

# Test environment mode
TEST_ENV=development

# =============================================================================
# OPTIONAL OVERRIDES
# =============================================================================

# Override backend URL for integration tests
# TEST_BACKEND_URL=http://localhost:3000

# Override frontend URL for E2E tests  
# TEST_FRONTEND_URL=http://localhost:9292

# Note: Tests will use mock data and fixtures by default.
# Only set these if testing against real services.